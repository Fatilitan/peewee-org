    # # If 'cheese' doesn't exist, create it
    # if kaas is None:
    #     kaas = models.Ingredient.create(
    #         name='cheese',
    #         is_vegetarian=True,
    #         is_vegan=True,
    #         is_glutenfree=True,
    #     )